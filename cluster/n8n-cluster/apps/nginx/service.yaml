apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: gitops
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - port: 8080        # service port (external)
      targetPort: 80     # container internal port
      nodePort: 30080    # NodePort (optional, manually set)

